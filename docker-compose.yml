version: '3.9'
services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
            target: prod
        ports:
            - 8000:8000
        container_name: test-container # TODO: Edit container name
